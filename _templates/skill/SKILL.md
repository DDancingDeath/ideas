---
name: {{SLUG}}
description: {{DESCRIPTION}}
---

# {{TITLE}}

> When to use this skill, when NOT to, any prerequisites.

## Quick start

The common-case recipe. Real commands or pseudocode the agent can copy.

```
TODO: replace with the actual happy-path recipe.
```

## 1. <First section>

The body. Use concrete content - actual commands, file paths, snippets.
Cite source-tree paths with `path:line` where the agent will need to
verify or act.

## 2. <Second section>

...

## Common pitfalls / FAQ

**Q: <a non-obvious trap>?**
A: <how to avoid or recover>.

---

> Authoring notes (delete before PR):
> - `name` in front-matter MUST equal the parent directory name (`{{SLUG}}`).
> - `description` should be 1-3 sentences, lead with the verb if action-oriented.
> - No `TODO`/placeholder strings in committed content - either flesh out, or
>   move the open question into an explicit "Open questions" list.
> - Update the relevant catalog (`skills/README.md` for the Hik repo) when
>   adding this skill via PR.
